{
  "name": "sg-gls-2h2026-map",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Interactive map of Singapore's Confirmed List sites in the 2H2026 Government Land Sales (GLS) Programme.",
  "license": "MIT",
  "scripts": {
    "validate": "node scripts/validate-data.mjs",
    "dev": "vite",
    "prebuild": "node scripts/validate-data.mjs",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "@types/geojson": "^7946.0.14",
    "@types/leaflet": "^1.9.12",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
